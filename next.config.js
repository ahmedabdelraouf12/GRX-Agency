/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'illustrations.popertee.com'],
  },
}

module.exports = nextConfig
